# Copy to `.env` (git-ignored) only if you want to change build-time settings.
# Secrets do NOT go here: API keys and tokens belong in `.dev.vars` (see `.dev.vars.example`).

# Which chat connections the build offers: both (default), direct (static-host build:
# no /api/*, the Server / Direct switch is hidden; `pnpm build:static` sets this), or server.
#PUBLIC_AI_CONNECTIONS=both

# Where the preview is rendered: browser (default, Web Worker) or server (/api/render, Worker Loader).
# `pnpm dev:server` sets this for you.
#PUBLIC_PREVIEW_RENDERER=browser

# Direct mode (browser → provider) reaches the Astro docs through a relay because the
# MCP server has no CORS headers. Default: this app's own /api/mcp-proxy. Point it at a
# stand-alone relay Worker when the front end is served statically.
#PUBLIC_DOCS_PROXY_URL=/api/mcp-proxy

# Origin that serves the preview sandbox frame (/preview/), where generated code runs.
# Unset: the dev server swaps localhost <-> 127.0.0.1 automatically; a production build
# without it renders in an in-origin Worker and the output badge says "not isolated".
# For a static host, deploy the same build to a second origin and name it here.
#PUBLIC_PREVIEW_ORIGIN=https://preview.example.com
